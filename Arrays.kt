/*Arreglos */
fun main(args: Array<String>) {
    var contacts = arrayOf("John", "James", "Amy") // Inicializar array
    var arreglo = intArrayOf(1,2,3) // Array numeros
    var arregloStg:Array<String> = arrayOf("A", "B")
    println(arregloStg[0]) // Acceder a un elemento por su index, en este caso, A
    println(arreglo[2]) // 3
}